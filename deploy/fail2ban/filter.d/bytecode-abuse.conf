# Fail2ban filter for bytecode.news nginx abuse log lines.
#
# Expected log format from deploy/nginx/abuse-http.conf:
# <IP> host=<host> status=<status> reason="<reason>" ...

[Definition]
failregex = ^<HOST>\s+host=.*\sstatus=403\sreason="(?:multipart|probe-path)".*$
ignoreregex =
